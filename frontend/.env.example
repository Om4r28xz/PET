# Supabase — required for real-time Event Gateway
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# API URLs — leave empty for local dev (Vite proxy handles it)
# Set these to your deployed Vercel URLs in production
VITE_NUTRITION_API_URL=
VITE_MEDICAL_API_URL=
